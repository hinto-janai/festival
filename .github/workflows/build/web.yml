name: Web Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Cache
      uses: actions/cache@v3.2.3
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: cargo build
    - uses: actions/checkout@v3
    - name: Build
      run: cd festival-web
      run: cargo build
